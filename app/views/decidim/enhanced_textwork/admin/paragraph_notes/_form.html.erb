<%= decidim_form_for(@notes_form, url: paragraph_paragraph_notes_path(paragraph, @notes_form), html: { class: "form new_paragraph_note" }) do |f| %>
  <div class="row column">
    <%= f.text_area :body, rows: 10, label: t(".note") %>
  </div>
  <div class="button--double form-general-submit">
    <%= f.submit t(".submit") %>
  </div>
<% end %>
